# Shared HMAC secret. Must match `STATISTICIAN_SHARED_SECRET` in the
# Next.js app's env. In Modal, set this via `modal secret create`.
STATISTICIAN_SHARED_SECRET=replace-with-32+-bytes-of-randomness

# Optional. Surfaced on /healthz so we can verify which build is live.
GIT_SHA=dev
